# Experiment Outputs

Large experiment outputs are intentionally excluded from version control.

This includes:

- trained model checkpoints
- prediction parquet files
- evaluation CSV outputs
- seed-robustness runs
- ablation and rolling-window result trees

The repository keeps only the source code and runner scripts required to regenerate those outputs locally.
